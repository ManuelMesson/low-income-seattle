import React, { Component } from "react";

class FreeMeals extends Component {
  componentDidMount = () => {
      fetch('')
  };
  render = () => {
    return <div />;
  };
}

export default FreeMeals;
